import React, { useState } from 'react';

// import { fadeIn } from "../animation";

export const SearchBar = () => {
  return (
    <nav>
      <div>
        <h1>Books API</h1>
      </div>
    </nav>
  );
};
